export interface IPricingCta {
  title: string;
  description: string;
  primaryButton: {
    text: string;
    icon: string;
  };
  secondaryButton: {
    text: string;
    icon: string;
  };
  disclaimer: string;
}
